// create a form that creates a new comparison document for my mongodb database
// the file should import the prisma client from lib/prisma.js and use it to add a new record to the database
import { ReadPreference } from 'mongodb';
import React, { useState } from 'react';
import { prisma } from '../../lib/prisma';

const AddComparisonForm = async () => {


  return (
    <div>
        
    </div>
  )
}

export default AddComparisonForm

